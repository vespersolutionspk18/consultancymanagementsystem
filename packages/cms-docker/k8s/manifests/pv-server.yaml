apiVersion: v1
kind: PersistentVolume
metadata:
  name: cmscrm-server-pv
  namespace: cmscrm
spec:
  storageClassName: default
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
