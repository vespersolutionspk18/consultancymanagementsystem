apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cmscrm-server-pvc
  namespace: cmscrm
spec:
  storageClassName: default
  volumeName: cmscrm-server-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
