{
  "setup-worktree": [
    "nvm use",
    "yarn",
    "cp $ROOT_WORKTREE_PATH/packages/cms-server/.env packages/cms-server/.env || true",
    "cp $ROOT_WORKTREE_PATH/packages/cms-front/.env packages/cms-front/.env || true"
  ]
}
